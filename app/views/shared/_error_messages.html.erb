<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(object.errors.count, "error") %>.
    </div>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<!-- pluralize([number], [singular string]):  ActionView::Helpers:TextHelper - returns properly pluralized 
     second argument including many irregular plurals.  e.g: 'woman' -> 'women'; 'erratum' -> 'errata' -->