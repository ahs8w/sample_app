<% provide(:title, "All users") %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
  <!-- infers that @users is a list of User objects, automatically iterates through list (@users.each do |user|) -->
  <!-- and renders each user variable (from User class) with the '_user.html.erb' partial -->
</ul>

<%= will_paginate %>